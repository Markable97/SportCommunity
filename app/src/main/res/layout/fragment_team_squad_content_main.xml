<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main2"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/fragment_team_squad">

    <com.tsuryo.swipeablerv.SwipeableRecyclerView
        android:id="@+id/squad_team_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:leftBgColor="@color/design_default_color_error"
        app:leftText="Удалить"
        app:rightBgColor="@color/blue"
        app:rightText="Отвязать"
        app:textColor="@android:color/white"
        app:textSize="14sp"
        tools:listitem="@layout/recycler_item_squad_list" />

</RelativeLayout>